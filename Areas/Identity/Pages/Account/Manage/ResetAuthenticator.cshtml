@page
@model ResetAuthenticatorModel
@{
	ViewData["Title"] = "Restablecer llave de autenticación en dos pasos (2FA)";
	ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
	<p>
		<span class="glyphicon glyphicon-warning-sign"></span>
		<strong>Si reestableces tu llave de autenticación en dos pasos (2FA), tu aplicación autenticaora no funcionará hasta que lo configures de nuevo.</strong>
	</p>
	<p>
		Este proceso deshabilita la autenticación en dos pasos hasta que verifiques tu aplicación.
		Si no completas la configuración de tu aplicación, podrías perder el acceso a tu cuenta.
	</p>
</div>
<div>
	<form id="reset-authenticator-form" method="post">
		<button id="reset-authenticator-button" class="btn btn-danger" type="submit">Reestablecer llave de autenticación en dos pasos (2FA)</button>
	</form>
</div>