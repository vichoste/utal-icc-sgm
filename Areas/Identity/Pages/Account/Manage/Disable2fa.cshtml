@page
@model Disable2faModel
@{
	ViewData["Title"] = "Deshabilitar autenticación en dos pasos (2FA)";
	ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
	<p>
		<strong>Esta acción sólo deshabilita la autenticación en dos pasos.</strong>
	</p>
	<p>
		Al deshabilitar la autenticación en dos pasos (2FA), ésta no cambia sus llaves de autenticación.
		Si lo deseas, puedes <a asp-page="./ResetAuthenticator">reestablecer tus llaves de autenticación.</a>
	</p>
</div>

<div>
	<form method="post">
		<button class="btn btn-danger" type="submit">Deshabilitar autenticación en dos pasos (2FA)</button>
	</form>
</div>