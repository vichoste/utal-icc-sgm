@page

@model LoginWithRecoveryCodeModel

@{
	ViewData["Title"] = "Verificación mediante código de recuperación";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
	Has solicitado iniciar sesión con un código de recuperación. Esta instancia de inicio de sesión no será
	recordada hasta que ingreses un código de autenticador en dos pasos (2FA) al inciar sesión o hasta que deshabilites
	la autenticación en dos pasos completamente e inicies sesión nuevamente.
</p>
<div class="row">
	<div class="col-md-4">
		<form method="post">
			<div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
			<div class="form-floating mb-3">
				<input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="012345" />
				<label asp-for="Input.RecoveryCode" class="form-label"></label>
				<span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
			</div>
			<button type="submit" class="w-100 btn btn-lg btn-primary">Iniciar sesión</button>
		</form>
	</div>
</div>