@using Utal.Icc.Sgm
@using Utal.Icc.Sgm.ViewModels
@using Utal.Icc.Sgm.ViewModels.Shared

@model Paginator<MemoirViewModel>

@{
	this.ViewData["Title"] = "Mis propuestas";
}

<div class="container">
	<partial name="_Alerts" />
	<div class="row justify-content-center">
		@if (this.User.IsInRole("Student"))
		{
			<div class="col-12 col-md-10 mb-3">
				<h2>@this.ViewData["Title"]</h2>
			</div>
			<div class="col-12 col-md-2 mb-3">
				@this.Html.ActionLink("Crear propuesta", "CreateStudentProposal", "Memoir", new { area = "University" }, new
			{ @class = "btn btn-success"})
			</div>
			<div class="col-12 mb-3">
				<partial name="_Filter" , model=@(new FilterPartialViewModel("StudentProposal", "Memoir", "University")) />
			</div>
		}
		else if (this.User.IsInRole("Guide"))
		{
			<div class="col-12 mb-3">
				<h2>@this.ViewData["Title"]</h2>
			</div>
			<div class="col-12 mb-3">
				<partial name="_Filter" , model=@(new FilterPartialViewModel("StudentProposal", "Memoir", "University")) />
			</div>
		}
	</div>
	<div class="row justify-content-center">
		<div class="col-12">
			<div class="table-responsive">
				<table class="table table-bordered">
					<thead class="table-dark">
						<tr>
							<th>
								<a asp-action="StudentProposal" asp-route-sortOrder="@this.ViewData["TitleSortParam"]"
									asp-route-currentFilter="@this.ViewData["CurrentFilter"]">
									Título
								</a>
							</th>
							<th>
								<a asp-action="StudentProposal"
									asp-route-sortOrder="@this.ViewData["FirstNameSortParam"]"
									asp-route-currentFilter="@this.ViewData["CurrentFilter"]">
									@if (this.User.IsInRole("Student"))
									{
										<span>Profesor guía</span>
									}
									else if (this.User.IsInRole("Guide"))
									{
										<span>Estudiante</span>
									}
								</a>
							</th>
							<th>Estado</th>
							<th>Acciones</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in this.Model)
						{
							<tr>
								<td>@item.Title</td>
								@if (this.User.IsInRole("Student"))
								{
									@item.GuideName
								}
								else if (this.User.IsInRole("Guide"))
								{
									@item.MemoristName
								}
								<td>
									@if (this.User.IsInRole("Student"))
									{
										@switch (item.Phase)
										{
											case "Draft":
												<span class="badge bg-secondary">Borrador</span>
												break;
											case "SentToGuide":
												<span class="badge bg-primary">Enviada</span>
												break;
											case "ApprovedByGuide":
												<span class="badge bg-success">Aceptada</span>
												break;
											case "RejectedByGuide":
												<span class="badge bg-danger">Rechazada</span>
												break;
										}
									}
									else if (this.User.IsInRole("Guide"))
									{
										@switch (item.Phase)
										{
											case "SentToGuide":
												<span class="badge bg-yellow">Pendiente de revisión</span>
												break;
											case "ApprovedByGuide":
												<span class="badge bg-success">Aceptada</span>
												break;
										}
									}
								</td>
								<td>
									@if (this.User.IsInRole("Student"))
									{
										@if (item.Phase == "Draft")
										{
											@this.Html.ActionLink("Editar", "EditStudentProposal", "Memoir", new { area =
								"University", id = item.Id }, new { @class = "btn btn-yellow mb-3"})
											@this.Html.ActionLink("Eliminar", "DeleteStudentProposal", "Memoir", new { area =
								"University", id = item.Id }, new { @class = "btn btn-danger mb-3"})
											@this.Html.ActionLink("Enviar", "SendStudentProposal", "Memoir", new { area =
								"University", id = item.Id }, new { @class = "btn btn-primary mb-3"})
										}
										else if (item.Phase == "SentToGuide")
										{
											@this.Html.ActionLink("Ver", "ViewStudentProposal", "Memoir", new { area = "University",
								id = item.Id }, new { @class = "btn btn-primary mb-3"})
										}
										else if (item.Phase == "ApprovedByGuide")
										{
											@this.Html.ActionLink("Convertir", "ConvertStudentProposal", "Memoir", new { area =
								"University", id = item.Id }, new { @class = "btn btn-success mb-3"})
										}
										else if (item.Phase == "RejectedByGuide")
										{
											@this.Html.ActionLink("Ver razón", "RejectedStudentProposal", "Memoir", new { area =
								"University", id = item.Id }, new { @class = "btn btn-danger mb-3"})
										}
									}
									else if (this.User.IsInRole("Guide"))
									{
										@if (item.Phase == "SentToGuide")
										{
											@this.Html.ActionLink("Ver", "ViewStudentProposal", "Memoir", new { area = "University",
								id = item.Id }, new { @class = "btn btn-yellow mb-3"})
										}
										else if (item.Phase == "ApprovedByGuide")
										{
											@this.Html.ActionLink("Ver", "ViewStudentProposal", "Memoir", new { area = "University",
								id = item.Id }, new { @class = "btn btn-success mb-3"})
										}
									}
								</td>
							</tr>
						}
					</tbody>
				</table>
				<partial name="_Paginator" , model=@(new PaginatorPartialViewModel("StudentProposal",
					this.Model.PageIndex, this.Model.TotalPages, this.Model.HasPreviousPage, this.Model.HasNextPage)) />
			</div>
		</div>
	</div>
</div>