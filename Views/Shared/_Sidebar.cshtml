@using Utal.Icc.Sgm.Models

<div id="sidebar" class="app-sidebar">
	<div class="app-sidebar-content" data-scrollbar="true" data-height="100%">
		<div class="menu">
			@if (this.User.Identity!.IsAuthenticated) {
				<div class="menu-header">
					Mis roles y herramientas
				</div>
			} else {
				<div class="menu-header">
					Debes iniciar sesión para visualizar tus roles y herramientas
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Director))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-school"></i></span>
						<span class="menu-text">Director de carrera</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action(nameof(Utal.Icc.Sgm.Areas.School.Controllers.UserController.Users), nameof(Utal.Icc.Sgm.Areas.School.Controllers.UserController).Replace("Controller", string.Empty), new { area = nameof(Utal.Icc.Sgm.Areas.School) })" class="menu-link">
								<span class="menu-text">Usuarios</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Courses", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Cursos</span>
							</a>
						</div>
					</div>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Committee))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-people-line"></i></span>
						<span class="menu-text">Profesor del comité</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action("Requests", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Solicitudes</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Users", "User", new { area = "School" })" class="menu-link">
								<span class="menu-text">Profesores</span>
							</a>
						</div>
					</div>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Course))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-person-chalkboard"></i></span>
						<span class="menu-text">Profesor del curso</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action("Courses", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Mis cursos</span>
							</a>
						</div>
					</div>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Guide))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-user-tie"></i></span>
						<span class="menu-text">Profesor guía</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action("Proposals", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Propuestas de estudiantes</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Proposals", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Mis propuestas</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Requests", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Nuestras solicitudes</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Users", "Memoir", new { area = "School" })"
								class="menu-link">
								<span class="menu-text">Mis memoristas</span>
							</a>
						</div>
					</div>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Assistant))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-user-tie"></i></span>
						<span class="menu-text">Profesor co-guía</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action("Users", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Mis memoristas</span>
							</a>
						</div>
					</div>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Engineer))) {
				<div class="menu-item">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-user-graduate"></i></span>
						<span class="menu-text">Ingeniero Civil en Computación</span>
					</a>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Completed))) {
				<div class="menu-item">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-user-graduate"></i></span>
						<span class="menu-text">Carrera completada</span>
					</a>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Memoir))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-user-graduate"></i></span>
						<span class="menu-text">Memorista</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action("Index", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Mi memoria de título</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Course", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Mi curso</span>
							</a>
						</div>
					</div>
				</div>
			}
			@if (this.User.IsInRole(nameof(Role.Student))) {
				<div class="menu-item has-sub">
					<a href="#" class="menu-link">
						<span class="menu-icon"><i class="fa fa-user-graduate"></i></span>
						<span class="menu-text">Estudiante</span>
						<span class="menu-caret"><b class="caret"></b></span>
					</a>
					<div class="menu-submenu">
						<div class="menu-item">
							<a href="@this.Url.Action("Proposals", "Memoir", new { area = "School" }) class="menu-link">
								<span class="menu-text">Mis propuestas</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Proposals", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Propuestas de profesores guía</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Requests", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Mis postulaciones</span>
							</a>
						</div>
						<div class="menu-item">
							<a href="@this.Url.Action("Requests", "Memoir", new { area = "School" })" class="menu-link">
								<span class="menu-text">Nuestras solicitudes</span>
							</a>
						</div>
					</div>
				</div>
			}
		</div>
	</div>
	<button class="app-sidebar-mobile-backdrop" data-dismiss="sidebar-mobile"></button>
</div>